03/22 - Amélioration de `<UiEmptyState>` avec `iconClass` et refactorisation des "empty states" (sans résultats, sans flux live, etc.) sur plusieurs pages (`stream/index.vue`, `stream/[id].vue`, `admin/dashboard.vue`, `weather.vue`). Règle établie : Éviter de dupliquer des blocs HTML de statut vide ; utiliser le composant `<UiEmptyState>` réutilisable avec des slots d'actions au besoin pour conserver l'uniformité du Glassmorphism.
